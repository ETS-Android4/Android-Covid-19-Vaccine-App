<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.notetakingapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoteTakingApp">
        <activity android:name=".UserTakeSurvey"></activity>
        <activity android:name=".UserRecieveConfirmation" />
        <activity android:name=".AdminSendConfirmation" />
        <activity android:name=".AdminSurvey" />
        <activity android:name=".AdminAppointments" />
        <activity android:name=".AdminPage" />
        <activity android:name=".Messages" />
        <activity android:name=".Appointments" />
        <activity android:name=".ScheduleAppointmentActivity" />
        <activity android:name=".ResetPassword" />
        <activity android:name=".UserPage" />
        <activity android:name=".NewUserActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>